node_modules
dist
out
.DS_Store
.eslintcache
*.log*
.env
.env.*
!.env.example

# Índice de embeddings del RAG (derivado; se construye con `npm run rag:build-embeddings`)
resources/knowledge/alagal_embeddings.json

# Set de validación derivado de presupuestos reales (datos sensibles del laboratorio)
resources/knowledge/cases_reales.json
